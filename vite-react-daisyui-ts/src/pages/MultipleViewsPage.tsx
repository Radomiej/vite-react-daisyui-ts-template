import { MultiViewGrid } from '../features/multiple-views';

export const MultipleViewsPage = () => {
  return (
    <div className="w-full h-full">
      <MultiViewGrid />
    </div>
  );
};

export default MultipleViewsPage;
